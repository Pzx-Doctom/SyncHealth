# SyncHealth 开发过程中的问题与解决方案

> 面试用：记录项目开发中遇到的核心技术难题及解决思路

---

## 一、数据导入性能优化（难度：★★★★★）

### 问题
导入 Apple Health 的 50,000 条健康记录需要 **20 分钟**，用户体验极差。

### 根因分析
原代码 `sync_service.py` 对每条记录执行 **先 SELECT 查重，再 INSERT 写入**：

```
for item in 5000条:
    SELECT ... WHERE sample_uuid=?    # 一次数据库往返
    INSERT INTO ... VALUES(...)       # 又一次数据库往返
```

5000 条记录 = 10,000 次数据库操作。SQLite 是单写锁，每条 INSERT 都锁表等 IO，是性能瓶颈。

### 解决方案
利用数据库表的 `UNIQUE(user_id, sample_uuid)` 约束，改为**批量插入 + 数据库自身去重**：

```sql
-- 之前：每批 5000 次 SELECT + 5000 次 INSERT = 10000 次操作
-- 之后：1 次 COUNT + 50 次批量 INSERT = 51 次操作
INSERT INTO heart_rates (...) VALUES (...), (...), ... 
ON CONFLICT(user_id, sample_uuid) DO NOTHING
```

### 衍生问题：SQLite 变量上限
单个 SQL 语句的变量数不能超过 999（SQLite 限制）。5000 条 × 8 列 = 40000 个变量 → 报错 `too many SQL variables`。

**解决**：每批拆为 100 条（800 变量），安全避开上限。

### 效果
导入时间从 **20 分钟降到 20 秒**，提升约 60 倍。

### 面试亮点
- 用数据库特性（UNIQUE 约束 + ON CONFLICT）代替应用层逻辑
- 发现问题→定位瓶颈→设计方案→处理边界情况（变量上限）的完整思路

---

## 二、多源数据重复计算（难度：★★★★）

### 问题
Dashboard 显示的步数/能量/距离**翻倍**，因为 iPhone 和 Apple Watch 各自独立上报同一次活动数据。

### 根因
Apple Health 导出文件包含多个设备来源的数据，每条记录有独立的 `sample_uuid`（不同设备生成不同 UUID），去重机制无法识别跨设备重复。

Dashboard 聚合查询没有任何来源过滤：
```sql
SELECT SUM(value) FROM activity_samples 
WHERE metric_type='steps' AND recorded_at >= 今天
-- 缺少 source_device 过滤！
```

### 解决方案
新增来源检测函数，优先使用 Apple Watch 数据（手表在手腕上，最准确）：

```python
async def _get_watch_filter(db, user_id, time_start):
    # 检查该时段是否有 Watch 数据
    count = SELECT COUNT(*) WHERE source_device LIKE '%watch%'
    return '%watch%' if count > 0 else None  # 有则只取 Watch
```

应用到所有 Dashboard 聚合查询（步数、能量、距离、趋势图、健康评分）。

### 面试亮点
- 从数据源头理解业务语义（HealthKit 多设备特性）
- 设计优雅的回退机制（Watch → 全源）
- 一次性覆盖所有关联查询，而非逐个打补丁

---

## 三、站立时间单位错误（难度：★★★）

### 问题
Dashboard 显示"当日站立 720 小时"，实际只站了 12 小时。

### 根因
`HKQuantityTypeIdentifierAppleStandTime` 的 XML 单位是 `min`（分钟），但代码中的 `metric_type` 命名为 `stand_hours`，直接将分钟数值当作小时使用，没有任何单位转换。

### 解决方案
两层面修复：
1. **解析层**：`parser.py` 新增 `min → hours` 单位转换（÷60），新导入数据直接正确
2. **展示层**：前端 Dashboard 和 ActivityView 加兼容判断 `val > 24 ? val/60 : val`，确保已入库的旧数据也能正确显示

### 面试亮点
- 同时处理新数据和存量数据的兼容
- 防御性的边界判断（一天不可能超过 24 小时）

---

## 四、WebSocket 流式输出与 Markdown 渲染（难度：★★★★）

### 问题 1：AI 回复是原始 Markdown，用户体验差
后端返回 `**加粗**`、`### 标题`、`| 表格 |` 等原始字符串，前端直接当纯文本显示。

**解决**：集成 `marked` 库，将 Markdown 实时转为 HTML 渲染，支持标题、表格、代码块、引用等全部 GFM 语法。

### 问题 2：回复是整段返回，体验不自然
原代码用 `POST /ai/chat` 一次性获取完整回复，等待时间长。

**发现**：后端其实已经实现了 WebSocket 流式端点 `WS /ai/chat/ws`，逐 token 推送，只是前端没对接。

### 问题 3：WebSocket 连接失败
前端通过 Vite 代理连接 WebSocket 时失败。

**根因**：Vite dev server 的 HTTP 代理默认不完全支持 WebSocket 升级协议转发。

**解决**：WebSocket 直连后端 `ws://127.0.0.1:8000`（WebSocket 不受浏览器 CORS 限制）。

### 最终效果
```
用户发消息 → 创建空 AI 气泡(闪烁光标|) → WS连接 → 后端逐字推送
→ "你" → "你的" → "你的心" → "你的心率" → ... → 完成
```

### 面试亮点
- 发现后端已有现成方案，只需前端对接（代码复用意识）
- 理解 WebSocket vs HTTP 的跨域差异
- 流畅的逐字输出体验设计

---

## 五、TCP 连接堆积导致服务假死（难度：★★★）

### 问题
后端启动正常（`Application startup complete`），`/docs` 能访问，但几分钟后**所有 API 请求无响应**，浏览器 `pending` 状态。

### 排查
1. 后端日志：`POST /login` → 无任何输出
2. 前端 Network：请求卡在 `pending`，无状态码
3. `netstat` 查端口：大量 `CLOSE_WAIT`（后端侧）和 `FIN_WAIT_2`（浏览器侧）连接堆积

### 根因
前端用绝对地址直连后端 8000 端口，每次请求都触发 CORS 预检（OPTIONS），浏览器反复重试导致 TCP 连接未释放，占满连接池。

### 解决
1. **立即恢复**：`taskkill` 强杀进程，清空连接
2. **根本修复**：前端 API 地址改为相对路径 `/api/v1`，走 Vite 代理（同域，无 CORS）

### 面试亮点
- 系统化排查思路：前端→网络→OS 层逐层定位
- 用 `netstat` 诊断 TCP 连接状态
- CORS 引发的 TCP 层面的连锁问题

---

## 六、前端功能补齐（难度：★★）

### 问题 1：Web 端无数据上传入口
原项目数据上传仅在已删除的 `mobile/` 目录，需要增加 Web 端 Apple Health 文件导入。

**解决**：新增 UploadView.vue + Apple Health API 封装，支持拖拽上传 .zip/.xml，后台异步解析并实时轮询进度。同时修复了 FormData 上传时全局 `Content-Type: application/json` 冲突问题。

### 问题 2：心率 TAB 三种类型混在一起
实时心率（几秒一条）、静息心率（一天几条）、步行心率（几天一条）混在一张图上，毫无意义。

**解决**：默认只展示 `heart_rate`（实时心率），增加日期选择器 + 客户端分页，新增"简略视图"——按 4 个时间段（0-6h/6-12h/12-18h/18-24h）展示每小时心率范围（min–max 横条）。

### 问题 3：活动 TAB 逻辑简陋
无日期筛选、无分页、多源重复。

**解决**：按心率 TAB 经验统一改造——日期选择 + Watch 来源过滤 + 每日汇总 + 分页表格 + 新增锻炼时长指标。

---

## 七、其他典型问题

| 问题 | 现象 | 解决方案 |
|---|---|---|
| FastAPI 根路径 404 | `GET /` 返回 404 | 添加 `@app.get("/")` 重定向到 `/docs` |
| 端口不一致 | 前后端各用 8001/8000 | 统一为 8000，修改配置 |
| `page_size` 传 10000 报错 | 后端限制 `le=1000` | 前端改 1000，后端改 `le=5000` |
| Dashboard 指标不统一 | 静息/步行心率无日期过滤 | 加 `recorded_at >= today_start` |
| 重构后变量残留 | `NameError: new_sessions not defined` | 代码审查不够仔细的教训 |

---

## 总结：解决这些问题的通用思路

1. **日志优先**：前端 F12 Network + Console，后端 uvicorn 输出 + SQLAlchemy 日志
2. **逐层排查**：前端 → 网络 → 后端路由 → Service → ORM → 数据库
3. **理解数据语义**：很多 Bug 不是代码问题，而是对 Apple HealthKit 数据格式理解不足
4. **优先级判断**：先恢复可用（taskkill/重启），再根本修复
5. **向后兼容**：改 parser 时考虑已入库的旧数据，加防御性判断
