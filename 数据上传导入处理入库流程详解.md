# SyncHealth 数据上传导入、处理、入库流程详解

## 一、整体架构概述

SyncHealth 是一个个人健康数据同步与分析平台，核心功能是将用户从 Apple HealthKit 导出的健康数据导入系统、清洗处理、去重入库，最终支持数据可视化和 AI 分析。

项目存在**两条独立的数据入口**，但最终汇聚到同一个核心处理函数，保证了数据一致性：

- **路径 A - Mobile 端实时同步**：React Native App 通过 HealthKit API 采集数据，以 JSON 格式直接发送
- **路径 B - Web 端文件导入**：用户在 Web 界面手动上传 Apple Health 导出的 XML/ZIP 文件

两条路径的关系如下图所示：

```
路径 A:  Mobile App ── REST API ──▶ POST /api/v1/sync/upload        ──▶ process_sync_upload() ──▶ SQLite
路径 B:  Web 前端   ── REST API ──▶ POST /api/v1/apple-health/upload ──▶ XML 解析               ──▶ process_sync_upload() ──▶ SQLite
```

在路径 B 中，XML 文件的解析是一个独立的子流程，负责将 Apple Health 原始格式转换为与路径 A 完全相同的内部数据结构。

---

## 二、路径 A：Mobile 端实时同步

### 2.1 入口：API 端点

**文件**: `backend/app/api/sync.py`

Mobile App 通过 `POST /api/v1/sync/upload` 发送数据。接口需要 JWT 身份认证，请求体为 `SyncPayload` JSON 对象。

与路径 B 不同，Mobile 端的同步是**同步调用**的——即在一次 HTTP 请求内完成所有入库操作并返回结果，没有后台任务。这是因为 Mobile 端每次同步的数据量通常较小（用户一天的增量数据），可以实时完成。

### 2.2 数据载体：SyncPayload

**文件**: `backend/app/schemas/sync.py`

`SyncPayload` 是整个系统的核心数据结构，无论数据来源是什么，最终都要转换为这个格式。它包含：

- `device_info`：设备信息（型号、系统版本、App 版本），记录在 `sync_logs` 表中
- `sync_window`：本次同步的时间范围（起止时间）
- 11 种健康数据类型的数组，每类都是一个 `list[PydanticModel]`：

| 字段名 | Pydantic 类型 | 说明 |
|--------|-------------|------|
| `heart_rates` | `list[HeartRateIn]` | 心率记录 |
| `hrv_samples` | `list[HRVSampleIn]` | 心率变异性 |
| `activity_samples` | `list[ActivitySampleIn]` | 活动数据（步数、距离、能量等） |
| `sleep_sessions` | `list[SleepSessionIn]` | 睡眠会话（含阶段子表） |
| `blood_oxygen_samples` | `list[BloodOxygenSampleIn]` | 血氧饱和度 |
| `body_temperature_samples` | `list[BodyTemperatureSampleIn]` | 体温 |
| `workout_records` | `list[WorkoutRecordIn]` | 运动记录（含心率区间子表） |
| `ecg_records` | `list[ECGRecordIn]` | 心电图记录 |
| `respiratory_rate_samples` | `list[RespiratoryRateSampleIn]` | 呼吸频率 |
| `noise_exposure_samples` | `list[NoiseExposureSampleIn]` | 环境噪音暴露 |
| `mindfulness_sessions` | `list[MindfulnessSessionIn]` | 正念训练 |

### 2.3 核心入库函数

**文件**: `backend/app/services/sync_service.py`

`process_sync_upload()` 是所有数据入库的统一入口，执行以下步骤：

1. **创建同步日志**：在 `sync_logs` 表中插入一条记录，标记状态为"进行中"
2. **分批入库简单表**：循环处理 9 种简单类型（无子表的数据），每种调用 `_bulk_insert()`
3. **分批入库嵌套表**：处理睡眠会话和运动记录两种有子表的结构
4. **更新元数据**：更新 `sync_logs` 状态为"完成"，记录统计数字；同时更新 `users.last_sync_at`

---

## 三、路径 B：Web 端文件导入（7 个阶段详解）

### 阶段 1：前端文件选择与校验

**文件**: `frontend/src/views/UploadView.vue`

用户在浏览器中选择或拖拽文件。前端在用户选择文件的瞬间就进行格式校验：只接受 `.xml` 和 `.zip` 后缀的文件，不通过则立即提示错误，不会发起网络请求。

选择文件后，界面会展示文件名和大小。用户点击"开始上传"按钮，前端通过 FormData 将文件发送到后端。

### 阶段 2：后端接收与存储

**文件**: `backend/app/api/apple_health.py` → `upload_apple_health()`

后端的处理逻辑：

1. **二次校验文件扩展名**（即使前端已校验，后端也做防御性检查）
2. **保存文件到磁盘**：路径为 `data/uploads/{task_id}_{原始文件名}`
3. **生成唯一的 `task_id`**：使用 `uuid.uuid4().hex`，用于后续状态查询
4. **初始化任务状态字典**：在内存字典 `_import_tasks` 中创建条目，状态为 `pending`
5. **投递后台任务**：通过 FastAPI 的 `BackgroundTasks` 机制异步执行 `_run_import_task()`
6. **立即返回**：接口不等待处理完成，直接返回 `{task_id, status: "pending"}`

这种设计的优势是：Apple Health 导出的 XML 文件可能非常大（数十 MB 甚至上百 MB），如果同步处理，HTTP 请求会超时。使用后台任务 + 轮询的方式，用户可以关闭页面稍后再来看结果。

> **注意**：任务状态字典存储在**内存**中，进程重启后会丢失。不过对应的 `sync_logs` 表记录会持久保存，所以导入历史不受影响。

### 阶段 3：ZIP 解压

**文件**: `backend/app/api/apple_health.py` → `_extract_if_zip()`

如果用户上传的是 `.zip` 文件，需要从中提取 XML。Apple Health 的导出 ZIP 内通常包含多个文件，解压逻辑按以下优先级选择：

1. `export.xml` — 标准导出文件名，最优先
2. `export_cda.xml` — CDA 格式的导出文件
3. 体积最大的 XML 文件 — 兜底策略，用文件大小推断主数据文件

解压到临时目录 `tempfile.mkdtemp()` 中，处理完成后会清理。如果文件本身就是 `.xml`，则跳过解压，直接使用原路径。

#### 阶段 3 → 阶段 4 的衔接方式

阶段 3 和阶段 4 在 `_run_import_task()` 中是**直接串联调用**的，没有任何中间层：

```
backend/app/api/apple_health.py → _run_import_task()

    # ── 阶段 3 ──
    xml_path, temp_dir = _extract_if_zip(file_path)
    #                    ^^^^^^^^^^^^^^^^^^^^^^^^^
    #                    返回值：(xml路径, 临时目录)
    #                    - .zip 文件 → 解压后的 XML 路径
    #                    - .xml 文件 → 原路径，temp_dir = None
    if not xml_path:
        _fail_task(task, "无法从上传文件中提取 XML")
        return

    # ── 阶段 4 ──
    for payload in parse_xml_to_payloads(xml_path):
    #                                     ^^^^^^^^
    #                    xml_path 直接作为参数传入解析器
        ...
```

`_extract_if_zip()` 返回一个 `(xml_path, temp_dir)` 元组，`xml_path` 是解压后（或原始）的 XML 文件**磁盘路径**。这个路径直接作为参数传入 `parse_xml_to_payloads(xml_path)`，后者内部通过 `create_parser(xml_path)` 打开文件开始流式解析。整个过程是一个简单的值传递，没有中间转换。

### 阶段 4：XML 流式解析

**文件**: `backend/app/services/apple_health/parser.py`

这是整个流程中最复杂的环节。Apple Health 的导出 XML 有两种格式：

- **Legacy 格式**（旧版 iOS）：根元素为 `<HealthData>`，数据在 `<Record>` 和 `<Workout>` 标签中
- **CDA 格式**（iOS 16+）：根元素为 `<ClinicalDocument>`，数据在 `<observation>` 标签中

解析器使用 `create_parser()` 工厂函数自动检测格式并返回对应的解析器实例。两个解析器都实现了**流式解析**（使用 `ET.iterparse`），逐条产出中间字典（dict），而不是一次性将整个 XML 加载到内存。这对于动辄上百 MB 的 XML 文件至关重要。

#### 4.1 时间解析

**文件**: `backend/app/services/apple_health/converter.py`

两种格式的时间表示不同：

- Legacy 格式：`"2024-01-01 08:00:00 +0800"` → 使用 `parse_iso_datetime()` 解析
- CDA 格式：`"20250717093819+0800"` → 使用 `parse_cda_datetime()` 解析，格式为 `YYYYMMDDHHMMSS+TZ`

两种解析器都支持多种时间格式的兼容处理，因为 Apple 在不同 iOS 版本的导出格式略有差异。

#### 4.2 类型路由分发

解析器根据 HealthKit 类型标识符将记录分发到对应的构建函数。以 Legacy 格式为例，`_parse_record()` 读取 `type` 属性后路由：

| HealthKit 类型标识符 | 调用函数 | 输出 type |
|---------------------|---------|----------|
| `HKQuantityTypeIdentifierHeartRate` / `RestingHeartRate` / `WalkingHeartRateAverage` | `_parse_heart_rate_record` | `heart_rate` |
| `HKQuantityTypeIdentifierHeartRateVariabilitySDNN` | `_parse_hrv_record` | `hrv` |
| `HKQuantityTypeIdentifierStepCount` / `DistanceWalkingRunning` / `ActiveEnergyBurned` / 等 7 种 | `_parse_activity_record` | `activity` |
| `HKCategoryTypeIdentifierSleepAnalysis` | `_parse_sleep_record` | `sleep_stage` |
| `HKQuantityTypeIdentifierOxygenSaturation` | `_parse_blood_oxygen_record` | `blood_oxygen` |
| `HKQuantityTypeIdentifierBodyTemperature` | `_parse_body_temperature_record` | `body_temperature` |
| `HKQuantityTypeIdentifierRespiratoryRate` | `_parse_respiratory_rate_record` | `respiratory_rate` |
| `HKCategoryTypeIdentifierElectrocardiogram` | `_parse_ecg_record` | `ecg` |
| `HKQuantityTypeIdentifierEnvironmentalAudioExposure` | `_parse_noise_exposure_record` | `noise_exposure` |
| `HKCategoryTypeIdentifierMindfulSession` | `_parse_mindfulness_record` | `mindfulness` |
| Workout 元素（独立于 Record） | `_parse_workout` | `workout` |

每条记录输出一个包含 `type` 和 `_counter_key` 的中间字典，这些字典在下一阶段被适配层消费。

#### 4.3 单位转换与数据清洗

**文件**: `backend/app/services/apple_health/converter.py` → `convert_unit()`

在解析过程中同步进行单位标准化，确保入库数据统一：

- 距离：`km → m`（行驶/跑步距离）、`mi → m`（英里转米）
- 能量：`kJ → kcal`（千焦转千卡）
- 温度：`degF → degC`（华氏度转摄氏度）
- 血氧：`fraction (0-1) → percent (0-100)`（Apple Health 原始存储为比例值）
- 站立时间：`min → hours`（分钟转小时）

#### 4.4 UUID 生成

每个记录需要一个全局唯一的 `sample_uuid` 用于后续去重。解析器优先使用 HealthKit XML 中自带的 `HKObjectUUID` 属性（Legacy 格式才有），如果没有（CDA 格式通常没有），则通过 `generate_uuid()` 生成一个基于内容的 MD5 哈希：

```
uuid = md5(record_type + "|" + start_date + "|" + value + "|" + source)
```

这意味着：同一个来源的同一条健康记录，即使多次导入，也会生成相同的 UUID，从而被去重机制自动跳过。

#### 4.5 运动记录的特殊处理

Workout（运动记录）在 Legacy XML 中不是 `<Record>` 而是独立的 `<Workout>` 元素，内部包含 `<Statistics>` 子元素。解析时需要额外提取：

- 运动类型（跑步、骑行、游泳等）→ 通过 `WORKOUT_TYPE_MAP` 映射
- 距离统计 → 从 `<Statistics type="HKQuantityTypeIdentifierDistanceWalkingRunning">` 提取
- 能量消耗 → 从 `<Statistics type="HKQuantityTypeIdentifierActiveEnergyBurned">` 提取

CDA 格式中，运动记录的处理方式不同——目前 CDA 解析器不处理 Workout（CDA 格式的 Workout 数据结构与 Legacy 差异较大）。

#### 4.6 心率的运动上下文推断

对于心率记录，解析器会从 `MetadataEntry` 中读取 `HKMetadataKeyHeartRateMotionContext` 的值来推断采样时用户的状态：

- `"1"` → `sedentary`（静止状态）
- `"2"` → `active`（运动状态）
- 其他 → `unset`（未设置）

这个信息在后续 AI 分析中有价值：同样是 120 bpm，静止状态下是异常，运动状态下则是正常的。

### 阶段 5：适配层——中间字典转 SyncPayload

**文件**: `backend/app/services/apple_health/adapter.py`

适配层的核心类是 `_PayloadAdapter`，它像一条流水线，消费解析器产出的中间字典，打包成标准化的 `SyncPayload`。

#### 5.1 分类型缓冲

`consume()` 方法根据中间字典的 `type` 字段，通过 `RECORD_TYPE_MAP` 找到对应的 Pydantic 模型类，然后将字典构造为模型实例，放入对应类型的缓冲区中。

睡眠阶段（`sleep_stage`）的处理比较特殊：不是立即转换，而是全部收集到 `_sleep_stages` 列表中，在最后一次性聚合。

#### 5.2 记录清洗

`_clean_record()` 函数执行三项清洗：

1. **剥离内部标签**：去除 `_counter_key`、`type`、`is_in_bed`、`is_asleep` 这些解析器内部使用但 SyncPayload 不需要的字段
2. **补全缺失字段**：Workout 和 Mindfulness 的中间字典没有 `recorded_at` 字段（它们只有 `start_time` 和 `end_time`），这里将 `start_time` 赋值给 `recorded_at`
3. **转换子结构**：Workout 的 `hr_zones` 从原始 dict 列表转换为 `WorkoutHRZoneIn` 模型列表

#### 5.3 分批输出

为了避免单个 `SyncPayload` 过大（XML 文件可能有几十万条记录），适配层每累积 **5000 条**非睡眠记录就调用 `flush_batch()` 输出一个 `SyncPayload`。这确保了：

- 内存占用可控（不需要一次性加载所有数据到内存）
- 数据库事务不会过大（每个 payload 独立提交）
- 前端可以实时看到进度更新

#### 5.4 睡眠会话聚合

**文件**: `backend/app/services/apple_health/sleep_aggregator.py`

Apple Health 的睡眠数据是分散的——每个睡眠阶段是一条独立记录（"22:00-23:00 core, 23:00-00:30 deep, ..."），但我们的数据库设计是"会话 + 阶段"的两层结构。

`aggregate_sleep_sessions()` 执行以下聚合逻辑：

1. **排序**：按 `start_time` 升序排列所有睡眠阶段
2. **切分会话**：相邻阶段的时间间隔超过 **2 小时**则视为两个不同的睡眠会话（比如午睡和夜间睡眠）
3. **构建会话**：每个会话取首阶段的 `start_time` 和末阶段的 `end_time`，计算总时长和在床时长
4. **UUID 继承**：会话的 `sample_uuid` 使用第一个阶段的 UUID

最终结果在 `flush_final()` 中追加到最后一个 SyncPayload 输出。

### 阶段 6：核心入库

**文件**: `backend/app/services/sync_service.py`

#### 6.1 去重机制

整个入库过程以 `(user_id, sample_uuid)` 组合作为唯一约束。使用 SQLite 的 `INSERT ... ON CONFLICT DO NOTHING` 语句实现：

```
INSERT INTO heart_rates (user_id, sample_uuid, bpm, ...)
VALUES (?, ?, ?, ...)
ON CONFLICT (user_id, sample_uuid) DO NOTHING
```

这意味着：
- 新记录 → 插入
- 已存在的记录（相同的 user_id + sample_uuid）→ 静默跳过，不报错

每次入库前先用 `COUNT(*) WHERE sample_uuid IN (...)` 统计已存在的数量，入库后计算 `inserted = received - existing`，从而准确追踪"接收了多少、实际写入了多少、跳过了多少"。

#### 6.2 批量插入优化（性能从 20 分钟 → 20 秒）

> **问题背景**：导入 Apple Health 的 50,000 条健康记录需要 **20 分钟**，用户体验极差。

##### 6.2.1 原始方案与根因

最早的 `_bulk_insert()` 对**每一条记录**执行"先查重、再插入"：

```
for item in 5000条记录:
    SELECT COUNT(*) WHERE sample_uuid = ?    ← 一次数据库往返
    INSERT INTO ... VALUES (...)             ← 又一次数据库往返
```

5000 条记录 = **10,000 次数据库操作**。SQLite 是单写锁数据库——每次 INSERT 都需要锁定整个数据库文件、等待磁盘 IO 完成、再释放锁。10,000 次操作串行执行，IO 等待时间累积起来就是 20 分钟。

##### 6.2.2 优化方案：利用数据库自身能力去重

核心思路是**让数据库去做去重，而不是应用层逐条判断**。数据库表的 `UNIQUE(user_id, sample_uuid)` 约束本身就是最好的去重工具。改进后的流程：

```
改进前：5000 条 × 2 次操作 = 10,000 次数据库往返
改进后：1 次 COUNT 统计 + (5000/100=50) 次批量 INSERT = 51 次操作

INSERT INTO heart_rates (...) VALUES
  (...), (...), ...    ← 一批 100 条
ON CONFLICT(user_id, sample_uuid) DO NOTHING
```

`ON CONFLICT DO NOTHING` 的含义是：如果这条记录的 `(user_id, sample_uuid)` 已经存在，静默跳过，不报错、不回滚。原本需要在 Python 代码中逐条 `SELECT` 判断的逻辑，现在由数据库引擎在底层一次性完成。

##### 6.2.3 衍生问题：SQLite 变量上限 999

改用批量 INSERT 后遇到了新问题：单个 SQL 语句的占位符变量不能超过 999 个（SQLite 编译时的硬限制）。

如果一次插入 5000 条，每条 8 列，就是 `5000 × 8 = 40,000` 个变量，远超 999，直接报错 `too many SQL variables`。

**解决**：将每批拆为 **100 条**（`100 × 8 = 800` 个变量），安全避开上限。这个数值经过精确计算：SQLite 上限 999 ÷ 每条记录最多的列数，取整后 100 是最优选择——批次越大 IO 次数越少，但必须留足安全余量。

##### 6.2.4 效果

导入时间从 **20 分钟降到 20 秒**，提升约 **60 倍**。

##### 6.2.5 设计思路的通用价值

这个优化的核心思想是**将应用层逻辑下沉到数据库层**：

- 不要用 Python 代码逐条判断数据是否重复 → 用 `UNIQUE` 约束 + `ON CONFLICT`
- 不要逐条 INSERT → 批量 VALUES
- 不要盲目相信数据库厂商的默认行为 → 了解底层限制（如 SQLite 变量上限）

##### 6.2.6 深入理解：为什么数据库内部还是要对比，速度却快了 60 倍？

一个容易产生的疑问是：`ON CONFLICT DO NOTHING` 背后，数据库不也要逐条检查 `(user_id, sample_uuid)` 是否存在吗？既然对比工作没少，为什么快这么多？

答案在于**在哪里对比**和**额外开销有多少**——对比本身不是瓶颈，瓶颈是对比之外的东西。

**（1）跨进程边界的往返次数**

改进前，每条记录需要 Python ↔ 数据库通信 2 次（先 SELECT 查重，再 INSERT）。虽然 SQLite 是嵌入式数据库、在同一进程内通过 C 扩展调用，但每次 `db.execute()` 仍然需要：

- Python 对象 → C 数据结构的序列化转换
- 独立的 SQL 语句解析和查询计划生成
- 结果集从 C 分配内存并构造 Python 对象

5000 条 × 2 次 = **10,000 次往返**。改进后，50 批 INSERT 只需要 **50 次往返**（外加 1 次 COUNT 统计），往返减少约 **200 倍**。

**（2）最大瓶颈：事务与磁盘刷写**

这是最关键的加速因素。改进前的逐条 INSERT，**每条默认是一个独立事务**：

```
每条 INSERT 的内部流程：
  1. BEGIN TRANSACTION
  2. 写入数据页
  3. 写入 WAL（预写日志）
  4. fsync() 刷盘            ← 等磁盘物理完成，约 1-10ms
  5. COMMIT
```

5000 条记录 = 5000 次 `fsync()`。即使每条只要 2ms，仅刷盘就累计 10 秒。实际加上索引 B-tree 的页面分配和更新，时间更长。

改进后的批量 INSERT 在同一个事务内完成（或更少的事务），50 批 = 最多 50 次 `fsync()`，**刷盘次数减少 99%**。

**（3）SQL 解析次数**

每条 `db.execute()` 都需要 SQLite 重新解析 SQL 文本、生成查询计划。改进前 10,000 次解析，改进后 51 次，减少约 200 倍。

**（4）索引查找的层级**

改进前：B-tree 索引查找在 C 层完成 → 结果返回 Python → Python 判断 → 再发 SQL → 再来一次 C 层。结果要在两层之间搬运。

改进后：查询和判断全部在 C 层内部闭环完成，Python 只管"发一批数据，告诉我最终结果"。

**对比总结**：

| 开销项 | 改进前（逐条 SELECT + INSERT） | 改进后（批量 ON CONFLICT） | 减少倍数 |
|--------|-------------------------------|--------------------------|---------|
| Python ↔ DB 往返 | 10,000 次 | 51 次 | ~200x |
| SQL 解析 | 10,000 次 | 51 次 | ~200x |
| 磁盘 fsync | ~5,000 次 | ~50 次 | ~100x |
| 结果对象构造 | 5,000 个 Python 对象 | 0（只有最后汇总） | — |

> **类比**：你去图书馆放 5000 本书。改进前是每次只拿一本书走到柜台、问管理员有没有重复、再拿一本放上去，来回了 5000 趟。改进后是一次抱 100 本给管理员说"都放进去，重复的扔掉"，管理员在柜台内部一本本处理——对比工作本身没少（还是 5000 本书），但你来回走的路从 5000 趟变成了 50 趟，而且管理员处理完一批才做一次签收登记（刷盘），不用每本书单独登记。

#### 6.3 简单表入库

9 种简单类型的表（心率、HRV、活动、血氧、体温、ECG、呼吸频率、噪音暴露、正念）使用统一的 `_bulk_insert()` 函数处理。流程为：

1. 提取所有记录的 `sample_uuid` 列表
2. 一次查询统计已存在数量
3. 分批构造 INSERT 语句并执行

#### 6.4 嵌套表入库：睡眠

睡眠数据入库分两步，使用 `_bulk_insert_sleep()`：

1. **插入会话主表**（`sleep_sessions`）：
   - 使用 `model_dump(exclude={"stages"})` 排除子表字段
   - `ON CONFLICT DO NOTHING` 处理重复
   - `await db.flush()` 触发 SQL 执行，获取自增 ID

2. **插入阶段子表**（`sleep_stages`）：
   - 回查刚插入的会话记录，获取 `session_id`
   - 为每个新插入的会话构建其阶段数据
   - 批量 INSERT 阶段数据

Flush 而不 Commit 的设计意味着：如果阶段插入失败，整个事务可以回滚，不会出现"有会话但没有阶段"的不一致状态。

#### 6.5 嵌套表入库：运动

运动记录（`_bulk_insert_workouts()`）的入库逻辑与睡眠完全相同——先插入主表（`workout_records`），flush 获取 ID，再回查后批量插入子表（`workout_hr_zones`）。

#### 6.6 元数据更新

入库完成后更新：
- `sync_logs` 表：标记 `status = "completed"`，记录插入/去重统计数字，保存设备信息 JSON
- `users` 表：更新 `last_sync_at` 为当前时间

### 阶段 7：状态轮询与清理

#### 7.1 前端轮询

**文件**: `frontend/src/views/UploadView.vue`

前端在上传成功后立即启动轮询，每 **2 秒**调用 `GET /api/v1/apple-health/status/{task_id}` 查询进度。界面实时展示：

- 任务状态（等待中 / 解析中 / 已完成 / 失败）
- 已接收记录数
- 已写入记录数
- 去重跳过数
- 处理批次数

当状态变为 `completed` 或 `failed` 时停止轮询，并刷新导入历史列表。

#### 7.2 资源清理

**文件**: `backend/app/api/apple_health.py` → `_cleanup()`

导入完成后（无论成功或失败），`_run_import_task()` 的 `finally` 块会执行清理：

1. 删除上传的原始文件（XML 或 ZIP）
2. 递归删除临时解压目录

临时文件不保留的原因：数据已经入库，原始文件没有保留价值，且 Apple Health 导出包含个人隐私数据。

---

## 四、数据模型与数据库表结构

所有表名及字段定义：

### 4.1 用户与日志

| 表名 | 所属文件 | 核心字段 | 说明 |
|------|---------|---------|------|
| `users` | `models/user.py` | `id`, `email`, `hashed_password`, `display_name`, `last_sync_at` | 用户账号，`last_sync_at` 记录最近同步时间 |
| `sync_logs` | `models/sync_log.py` | `id`, `user_id`, `started_at`, `completed_at`, `status`, `records_received/inserted/deduplicated`, `error_message`, `client_metadata` | 每次同步操作的日志 |

### 4.2 心脏数据

| 表名 | 所属文件 | 核心字段 | 唯一约束 |
|------|---------|---------|---------|
| `heart_rates` | `models/heart.py` | `bpm`, `motion_context`(sedentary/active/unset), `measurement_type`(heart_rate/resting/walking) | `(user_id, sample_uuid)` |
| `hrv_samples` | `models/heart.py` | `sdnn_ms` | `(user_id, sample_uuid)` |

### 4.3 活动数据

| 表名 | 所属文件 | 核心字段 | 说明 |
|------|---------|---------|------|
| `activity_samples` | `models/activity.py` | `metric_type`(steps/distance_meters/active_energy_kcal/resting_energy_kcal/stand_hours/flights_climbed/exercise_time), `value`, `duration_seconds` | 7 种活动指标统一存储 |

### 4.4 睡眠数据

| 表名 | 所属文件 | 核心字段 | 关系 |
|------|---------|---------|------|
| `sleep_sessions` | `models/sleep.py` | `start_time`, `end_time`, `total_duration_minutes`, `in_bed_duration_minutes` | 一对多 → `sleep_stages` |
| `sleep_stages` | `models/sleep.py` | `session_id`(FK), `stage`(awake/rem/core/deep), `start_time`, `end_time`, `duration_minutes` | |

### 4.5 生命体征

| 表名 | 所属文件 | 核心字段 |
|------|---------|---------|
| `blood_oxygen_samples` | `models/vitals.py` | `spo2_percent`(0-100) |
| `body_temperature_samples` | `models/vitals.py` | `temperature_celsius` |
| `respiratory_rate_samples` | `models/vitals.py` | `breaths_per_minute` |
| `ecg_records` | `models/vitals.py` | `classification`, `average_heart_rate` |

### 4.6 运动数据

| 表名 | 所属文件 | 核心字段 | 关系 |
|------|---------|---------|------|
| `workout_records` | `models/workout.py` | `workout_type`, `start_time`, `end_time`, `duration_seconds`, `active_energy_kcal`, `distance_meters` | 一对多 → `workout_hr_zones` |
| `workout_hr_zones` | `models/workout.py` | `workout_id`(FK), `zone_index`, `lower_bpm`, `upper_bpm`, `duration_seconds` | |

### 4.7 环境与心理健康

| 表名 | 所属文件 | 核心字段 |
|------|---------|---------|
| `noise_exposure_samples` | `models/environment.py` | `decibels`, `duration_seconds` |
| `mindfulness_sessions` | `models/environment.py` | `start_time`, `end_time`, `duration_minutes` |

---

## 五、完整函数调用关系图

### 路径 A：Mobile 同步（JSON）

```
frontend (Mobile App) → HTTP POST /api/v1/sync/upload

upload_sync_data()                                  [backend/app/api/sync.py]
  │
  └──▶ process_sync_upload(db, payload, user_id)   [backend/app/services/sync_service.py]
         │
         ├──▶ SyncLog(...)                           [backend/app/models/sync_log.py]
         │
         ├──▶ _bulk_insert() × 9                    [backend/app/services/sync_service.py]
         │      │
         │      ├──▶ select(count) WHERE uuid IN...  (SQL 去重统计)
         │      └──▶ insert(...) ON CONFLICT DO NOTHING (SQL 批量插入)
         │
         ├──▶ _bulk_insert_sleep()                  [backend/app/services/sync_service.py]
         │      ├──▶ insert(SleepSession)
         │      ├──▶ db.flush()                     (获取自增 ID)
         │      ├──▶ select(id, uuid)               (查回新插入记录)
         │      └──▶ insert(SleepStage)             (子表批量写入)
         │
         ├──▶ _bulk_insert_workouts()               [backend/app/services/sync_service.py]
         │      ├──▶ insert(WorkoutRecord)
         │      ├──▶ db.flush()
         │      ├──▶ select(id, uuid)
         │      └──▶ insert(WorkoutHRZone)
         │
         └──▶ db.get(User, user_id)                 (更新 last_sync_at)
```

### 路径 B：Web 文件导入（XML/ZIP）

```
UploadView.vue                                     [frontend/src/views/UploadView.vue]
  │
  ├──▶ pickFile(f)                                  (前端: 校验文件格式)
  │
  └──▶ startUpload()
         │
         ├──▶ appleHealthApi.upload(file)           [frontend/src/api/appleHealth.ts]
         │      → POST /api/v1/apple-health/upload
         │
         └──▶ startPolling(taskId)                  (前端: 每2秒轮询状态)
                └──▶ appleHealthApi.getStatus()      [frontend/src/api/appleHealth.ts]
                       → GET /api/v1/apple-health/status/{id}

────────────────────── 后端 ──────────────────────────

upload_apple_health()                              [backend/app/api/apple_health.py]
  │
  ├──▶ os.makedirs(upload_dir)                     (创建上传目录)
  ├──▶ shutil.copyfileobj(...)                      (保存文件到磁盘)
  │
  └──▶ background_tasks.add_task(
         _run_import_task)                          [backend/app/api/apple_health.py]
         │
         ├──▶ _extract_if_zip(file_path)            [backend/app/api/apple_health.py]
         │      └──▶ zipfile.ZipFile + namelist()  (ZIP 解压 & 选择 XML)
         │
         ├──▶ parse_xml_to_payloads(xml_path)       [backend/app/services/apple_health/adapter.py]
         │      │
         │      ├──▶ create_parser(xml_path)         [backend/app/services/apple_health/parser.py]
         │      │      │
         │      │      └──▶ detect_format()          [backend/app/services/apple_health/parser.py]
         │      │            → 返回 CDAHealthXMLParser 或 LegacyHealthXMLParser
         │      │
         │      ├──▶ parser.parse()                  [backend/app/services/apple_health/parser.py]
         │      │      │
         │      │      │  === CDA 格式分支 ===
         │      │      ├──▶ _parse_observation()
         │      │      │      │
         │      │      │      ├──▶ parse_cda_datetime()        [converter.py]
         │      │      │      ├──▶ generate_uuid()             [converter.py]
         │      │      │      │
         │      │      │      ├──▶ _build_activity()           [parser.py]
         │      │      │      │      └──▶ convert_unit()       [converter.py]
         │      │      │      ├──▶ _build_heart_rate()         [parser.py]
         │      │      │      │      ├──▶ infer_motion_context()[converter.py]
         │      │      │      │      └──▶ get_heart_rate_measurement_type() [converter.py]
         │      │      │      ├──▶ _build_hrv()                [parser.py]
         │      │      │      ├──▶ _build_blood_oxygen()       [parser.py]
         │      │      │      ├──▶ _build_body_temperature()   [parser.py]
         │      │      │      │      └──▶ convert_unit()       [converter.py]
         │      │      │      ├──▶ _build_respiratory_rate()   [parser.py]
         │      │      │      ├──▶ _build_noise_exposure()     [parser.py]
         │      │      │      ├──▶ _build_sleep()              [parser.py]
         │      │      │      │      └──▶ get_sleep_stage()    [converter.py]
         │      │      │      ├──▶ _build_mindfulness()        [parser.py]
         │      │      │      └──▶ _parse_value()              [parser.py]
         │      │      │
         │      │      │  === Legacy 格式分支 ===
         │      │      ├──▶ _parse_record(elem)
         │      │      │      │
         │      │      │      ├──▶ _get_uuid()                 [parser.py]
         │      │      │      │      └──▶ generate_uuid()      [converter.py]
         │      │      │      │
         │      │      │      ├──▶ _parse_activity_record()    [parser.py]
         │      │      │      │      ├──▶ convert_unit()       [converter.py]
         │      │      │      │      └──▶ parse_duration_seconds() [converter.py]
         │      │      │      │             └──▶ parse_iso_datetime() [converter.py]
         │      │      │      ├──▶ _parse_heart_rate_record()  [parser.py]
         │      │      │      │      ├──▶ _extract_metadata()  [parser.py]
         │      │      │      │      ├──▶ infer_motion_context()[converter.py]
         │      │      │      │      └──▶ get_heart_rate_measurement_type() [converter.py]
         │      │      │      ├──▶ _parse_hrv_record()         [parser.py]
         │      │      │      ├──▶ _parse_sleep_record()       [parser.py]
         │      │      │      │      ├──▶ get_sleep_stage()    [converter.py]
         │      │      │      │      └──▶ parse_iso_datetime() [converter.py]
         │      │      │      ├──▶ _parse_blood_oxygen_record() [parser.py]
         │      │      │      ├──▶ _parse_body_temperature_record() [parser.py]
         │      │      │      │      └──▶ convert_unit()       [converter.py]
         │      │      │      ├──▶ _parse_respiratory_rate_record() [parser.py]
         │      │      │      ├──▶ _parse_ecg_record()         [parser.py]
         │      │      │      │      └──▶ _extract_metadata()  [parser.py]
         │      │      │      ├──▶ _parse_noise_exposure_record() [parser.py]
         │      │      │      └──▶ _parse_mindfulness_record() [parser.py]
         │      │      │
         │      │      └──▶ _parse_workout(elem)              [parser.py]
         │      │             ├──▶ get_workout_type()          [converter.py]
         │      │             ├──▶ parse_iso_datetime()        [converter.py]
         │      │             └──▶ convert_unit()              [converter.py]
         │      │
         │      ├──▶ _PayloadAdapter.consume(record)          [adapter.py]
         │      │      │
         │      │      ├──▶ RECORD_TYPE_MAP 查询             (类型→模型映射)
         │      │      ├──▶ _clean_record(record)             [adapter.py]
         │      │      │      (剥离内部字段, 补 recorded_at)
         │      │      └──▶ model_cls(**cleaned)              (Pydantic 构造)
         │      │
         │      ├──▶ _PayloadAdapter.flush_batch()            [adapter.py]
         │      │      (每 5000 条 yield 一个 SyncPayload)
         │      │
         │      └──▶ _PayloadAdapter.flush_final()            [adapter.py]
         │             │
         │             └──▶ aggregate_sleep_sessions()        [sleep_aggregator.py]
         │                    │
         │                    ├──▶ 按 start_time 排序
         │                    ├──▶ 按 2h 间隔切分会话
         │                    └──▶ _build_session()           [sleep_aggregator.py]
         │
         ├──▶ process_sync_upload(db, payload, user_id)      [sync_service.py]
         │      │
         │      ├──▶ _bulk_insert() × 9                       [sync_service.py]
         │      ├──▶ _bulk_insert_sleep()                     [sync_service.py]
         │      └──▶ _bulk_insert_workouts()                  [sync_service.py]
         │
         └──▶ _cleanup(file_path, temp_dir)                  [app/apple_health.py]
                ├──▶ os.remove(file_path)
                └──▶ shutil.rmtree(temp_dir)
```

---

## 六、按文件汇总的函数职责

| 文件 | 函数 | 职责 |
|------|------|------|
| **frontend/src/views/UploadView.vue** | `onFileChange` | 响应文件选择事件 |
| | `onDrop` | 响应拖拽事件 |
| | `pickFile` | 校验文件后缀（.xml/.zip） |
| | `startUpload` | 发起上传请求 + 启动轮询 |
| | `startPolling` | 每 2 秒查询任务进度 |
| | `loadHistory` | 加载导入历史列表 |
| **frontend/src/api/appleHealth.ts** | `appleHealthApi.upload` | POST 上传文件（FormData） |
| | `appleHealthApi.getStatus` | GET 查询任务状态 |
| | `appleHealthApi.getHistory` | GET 查询导入历史 |
| **backend/app/api/sync.py** | `upload_sync_data` | 路径A：接收 Mobile JSON 数据 |
| | `get_sync_status` | 查询最近同步状态 |
| | `get_sync_history` | 分页查询同步历史 |
| **backend/app/api/apple_health.py** | `upload_apple_health` | 路径B：接收上传文件 + 投递后台任务 |
| | `get_import_status` | 查询导入任务进度（内存） |
| | `get_import_history` | 查询导入历史（SyncLog） |
| | `_run_import_task` | 后台任务主函数：解压→解析→入库→清理 |
| | `_extract_if_zip` | ZIP 解压 + 优先级选择 XML |
| | `_fail_task` | 标记任务失败 |
| | `_cleanup` | 删除临时文件 |
| **backend/app/services/sync_service.py** | `process_sync_upload` | 核心入库函数：创建日志→分类写入→更新状态 |
| | `_bulk_insert` | 简单表批量插入（分批 100 条、去重） |
| | `_bulk_insert_sleep` | 睡眠会话+阶段嵌套写入 |
| | `_bulk_insert_workouts` | 运动记录+心率区间嵌套写入 |
| **backend/app/services/apple_health/adapter.py** | `parse_xml_to_payloads` | XML → SyncPayload 的流式转换入口 |
| | `_PayloadAdapter.consume` | 消费单条中间字典，按类型缓冲 |
| | `_PayloadAdapter.flush_batch` | 每 5000 条输出一个 SyncPayload |
| | `_PayloadAdapter.flush_final` | 输出剩余数据 + 聚合后的睡眠 |
| | `_clean_record` | 记录清洗：剥内部标签、补字段 |
| **backend/app/services/apple_health/parser.py** | `create_parser` | 工厂函数：检测格式 → 返回解析器实例 |
| | `detect_format` | 检测 XML 根元素，返回 cda/legacy/unknown |
| | `CDAHealthXMLParser.parse` | CDA 格式流式解析 |
| | `CDAHealthXMLParser._parse_observation` | 解析单个 CDA observation 元素 |
| | `CDAHealthXMLParser._build_activity` 等 | 各类型记录的构建函数 |
| | `LegacyHealthXMLParser.parse` | Legacy 格式流式解析 |
| | `LegacyHealthXMLParser._parse_record` | 解析单个 Record 元素（路由分发） |
| | `LegacyHealthXMLParser._parse_activity_record` 等 | 各类型记录的解析函数 |
| | `LegacyHealthXMLParser._parse_workout` | Workout 元素解析（含 Statistics） |
| | `LegacyHealthXMLParser._extract_metadata` | 提取 Record 下的 MetadataEntry |
| | `parse_cda_datetime` | 解析 CDA 格式时间字符串 |
| **backend/app/services/apple_health/converter.py** | `convert_unit` | 单位转换（km→m, kJ→kcal, degF→degC 等） |
| | `parse_iso_datetime` | 解析 ISO 时间字符串（多种格式兼容） |
| | `parse_duration_seconds` | 计算两个时间之间的秒数差 |
| | `generate_uuid` | 基于内容 MD5 生成唯一标识 |
| | `get_sleep_stage` | HealthKit 睡眠值 → 内部 stage |
| | `get_workout_type` | HealthKit 运动类型 → 内部 type |
| | `get_heart_rate_measurement_type` | HealthKit 心率类型 → 内部类型 |
| | `infer_motion_context` | 从元数据推断运动上下文 |
| **backend/app/services/apple_health/sleep_aggregator.py** | `aggregate_sleep_sessions` | 将散落阶段按 2h 间隔聚合为会话 |
| | `_build_session` | 构建单个睡眠会话 dict |

---

## 七、关键设计决策与考量

### 7.1 为什么 XML 导入用后台任务而不是同步处理

Apple Health 导出的 XML 文件可能非常大（包含数年数据时可达数百 MB）。同步处理会导致：
- HTTP 请求超时（默认 FastAPI 超时 60 秒）
- 用户需要一直保持页面开启
- 无法提供进度反馈

使用后台任务 + 轮询模式，用户可以发起导入后关闭页面，稍后再查看结果。

### 7.2 为什么分批输出 SyncPayload 而不是一次性构建

`_PayloadAdapter` 每 5000 条记录就输出一个 SyncPayload，原因有三：

1. **内存控制**：数十万条记录一次性加载会占用大量内存
2. **事务粒度**：每个 payload 独立提交事务，单个失败不影响其他
3. **进度可见**：前端可以实时看到已处理批次数的增长

### 7.3 为什么睡眠阶段要最后聚合而不是逐条入库

Apple Health 的睡眠数据是分散的独立记录，但我们的数据库设计是"会话包含阶段"的两层结构。如果在解析过程中就入库，会因为缺少上下文而无法正确切分会话（不知道后续还有没有阶段属于同一个会话）。

因此适配层将睡眠阶段全部缓冲，在 `flush_final()`（解析结束时）一次性聚合后再入库。由于睡眠数据总量通常不大（每天最多几条），内存开销可以接受。

### 7.4 去重策略的可靠性

去重完全依赖 `(user_id, sample_uuid)` 唯一约束。UUID 的来源：

- **Legacy XML 优先取 HealthKit 原生 UUID**：保证即使多次导出同一批数据，UUID 也不变
- **无原生 UUID 时用内容哈希**：`md5(type + date + value + source)`，相同记录生成相同哈希

这种策略意味着：用户重复导入同一个导出文件，所有记录都会被识别为重复并被跳过，不会产生冗余数据。

### 7.5 任务状态存储的选择

当前任务状态存储在内存字典 `_import_tasks` 中（而非数据库或 Redis）。这是因为：
- 导入任务是临时的（完成后即无意义）
- 永久记录已保存在 `sync_logs` 表中
- 进程重启后状态丢失影响不大（用户可重新上传）

未来如果需要支持分布式部署，可以迁移到 Redis。

### 7.6 单位转换在解析阶段而非入库阶段

所有单位转换（km→m、kJ→kcal 等）都在 XML 解析阶段完成，而不是入库阶段。这样做的优势是：
- 数据库中的数据始终是标准单位，查询时不需要额外转换
- 路径 A 的 Mobile 端也发送标准单位，两路径数据一致
- 符合"数据规范化在入口处完成"的设计原则

---

## 八、数据采集规则参考

**文件**: `数据采集规则.md`

项目中记录的数据采集规格：

| HealthKit 标识符 | 类型 | 采集频率 |
|-----------------|------|---------|
| `HKQuantityTypeIdentifierHeartRate` | 实时心率 | ~每 10 分钟一次（静止），运动时连续 |
| `HKQuantityTypeIdentifierRestingHeartRate` | 静息心率 | 每天 1 条 |
| `HKQuantityTypeIdentifierWalkingHeartRateAverage` | 步行心率 | 每天 1 条 |
| `HKQuantityTypeIdentifierStepCount` | 步数 | 高频 |
| `HKQuantityTypeIdentifierFlightsClimbed` | 爬楼层数 | 按事件 |
| `HKQuantityTypeIdentifierDistanceWalkingRunning` | 步行/跑步距离 | 高频 |
| `HKCategoryValueSleepAnalysis*` | 睡眠阶段 | 夜间，分 core/deep/rem/awake |
